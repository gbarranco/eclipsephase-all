{% extends 'base.html.twig' %}

{% block title %}{% trans %}Edit Character{% endtrans %} : {{ character.characterName }}{% endblock %}

{% block body %}
    <h1>{% trans %}Edit Character{% endtrans %} : {{ character.characterName }}</h1>

    {{ include('character/_form.html.twig') }}

    <div class="back">
        <a href="{{ path('character_index') }}">
            <i class="fas fa-angle-double-left">
                {% trans %}back to list{% endtrans %}
            </i>
        </a>
    </div>

    {{ include('character/_delete_form.html.twig') }}
{% endblock %}
