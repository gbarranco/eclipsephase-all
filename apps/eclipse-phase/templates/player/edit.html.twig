{% extends 'base.html.twig' %}

{% block title %}{% trans %}Edit Player{% endtrans %} : {{ player.playerName }}{% endblock %}

{% block body %}
    <h1>{% trans %}Edit Player{% endtrans %} : {{ player.playerName }}</h1>

    {{ include('player/_form.html.twig') }}

    <div class="back">
        <a href="{{ path('player_index') }}">
            <i class="fas fa-angle-double-left">
                {% trans %}back to list{% endtrans %}
            </i>
        </a>
    </div>

    {{ include('player/_delete_form.html.twig') }}
{% endblock %}
